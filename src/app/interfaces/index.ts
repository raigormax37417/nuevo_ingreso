export interface Alumno {
    id: string;
    nombre: string;
    email: string;
    semestre: number;
    matricula: number;
    nivel_academico: string;
    especialidad: string;
}
export interface User {
    uid: string;
    email: string;
}